import type { ReactNode } from "react";

export default function H4({ children }: { children: ReactNode }) {
  return <h4 className="font-playfairdisplay text-[32px] lg:text-[36px] text-[#FFFFFF]">{children}</h4>;
}
